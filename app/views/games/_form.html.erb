<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
        <% game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group'>
    <%= form.file_field :images, multiple: true, class: "form-control-file btn btn-sm"%>
  </div>

  <div class="actions">
    <%= form.submit 'Start New Game', class: "btn btn-outline-primary btn-lg mt-5" %>
  </div>
<% end %>

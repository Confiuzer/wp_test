<h1>Games Played</h1>

  <ul>
    <% @games.each do |game| %>
      <li><%= game.created_at.strftime("%B %d, %Y") %></li>
    <% end %>
  </ul>

  <p>Please upload the images to play with in your next game:</p>
  <%= render 'games/form', game: @game %>
